{% trans_default_domain classes.transDomain|default('Department') %}

{% if classes.classes|length > 0 %}
<div class="column-no-break">
    <h3>{{ 'My Classes'|trans }}</h3>
    <ul style="list-style: none;">
        {% for class in classes.classes %}
            <li><a href="{{ path('course_class_edit', {course: class.course.id, class: class.id, tabName: 'Details'}) }}">{{ class.getAbbreviatedName()}}</a></li>
        {% endfor %}
    </ul>
</div>
{% endif %}